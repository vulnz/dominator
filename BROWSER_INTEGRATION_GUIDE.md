# Browser Integration Module - User Guide

## Быстрый старт / Quick Start

### Шаг 1: Запуск GUI
```bash
python main.py --gui
```

### Шаг 2: Открыть вкладку "🌐 Browser & Proxy"
В GUI перейдите на последнюю вкладку с названием "🌐 Browser & Proxy"

### Шаг 3: Запустить прокси
1. Нажмите кнопку **"▶ Start Proxy"**
2. Прокси запустится на порту **8080** (по умолчанию)
3. Статус изменится на **🟢 Proxy: Running**

### Шаг 4: Запустить браузер
1. Нажмите кнопку **"🌐 Launch Chrome Browser"**
2. Chrome откроется с автоматически настроенным прокси
3. Все запросы будут автоматически перехватываться

### Шаг 5: Начать работу!
Теперь просто используйте браузер - все запросы будут логироваться в истории!

---

## Основные функции / Main Features

### 🔍 Перехват запросов (Request Interception)

**Как включить:**
1. Поставьте галочку **"Enable Intercept"**
2. При каждом запросе будет появляться диалог

**Действия в диалоге перехвата:**
- ✓ **Forward** - Отправить запрос без изменений
- ✏ **Modify & Forward** - Изменить запрос и отправить
- ✗ **Drop** - Отбросить запрос (не отправлять)

**Пример использования:**
1. Включите Intercept
2. Зайдите на сайт http://testphp.vulnweb.com
3. Появится диалог с перехваченным запросом
4. Можете изменить заголовки, тело запроса, URL
5. Нажмите "Modify & Forward" для отправки

---

### 📜 История запросов (Request History)

**Что показывается:**
- **#** - Номер запроса
- **Time** - Время запроса
- **Method** - GET, POST, PUT, DELETE, etc.
- **URL** - Полный URL запроса
- **Status** - HTTP статус код (200, 404, 500, etc.)
- **Length** - Размер ответа в байтах
- **Notes** - Заметки (можно добавить вручную)

**Как использовать:**
1. Кликните на любой запрос в таблице
2. Ниже появятся вкладки Request/Response с полными данными
3. Можете скопировать данные для анализа

**Цветовая индикация статусов:**
- 🟢 **Зеленый** (200-299) - Успешный запрос
- 🔵 **Синий** (300-399) - Редирект
- 🟠 **Оранжевый** (400-499) - Ошибка клиента
- 🔴 **Красный** (500+) - Ошибка сервера

---

### ↻ Replay запросов (Request Replay)

**Простой replay:**
1. Выберите запрос из истории
2. Нажмите **"↻ Replay Request"**
3. Запрос будет отправлен снова
4. Появится результат (статус и размер ответа)

**Replay с модификацией:**
1. Выберите запрос из истории
2. Нажмите **"✏ Modify & Replay"**
3. Откроется редактор запроса
4. Измените что нужно (URL, headers, body)
5. Нажмите "OK" для отправки

**Примеры использования:**
- Тестирование SQL инъекций вручную
- Изменение параметров для обхода фильтров
- Добавление кастомных заголовков
- Изменение методов (GET → POST)

---

### 🔍 Send to Scanner (Authenticated Scanning)

**NEW FEATURE!** Автоматическая настройка сканера с cookies и заголовками из перехваченного запроса.

**Что это:**
Отправка выбранного запроса в сканер с автоматической конфигурацией всех cookies и custom headers.
Идеально подходит для тестирования аутентифицированных страниц и защищенных endpoint'ов.

**Как использовать:**
1. Авторизуйтесь на сайте через браузер с прокси
2. Выполните запрос к защищённой странице (например, /dashboard, /admin)
3. В истории выберите этот запрос (должен содержать session cookie)
4. Нажмите **"🔍 Send to Scanner"** (оранжевая кнопка)
5. Откроется диалог конфигурации сканирования

**Диалог конфигурации показывает:**
- **Request Information:** URL, Method, количество cookies и headers
- **Module Selection:** Выбор модулей для сканирования (SQLi, XSS, etc.)
  - По умолчанию выбраны все модули
  - Кнопки "Select All" / "Deselect All"
  - 3 колонки с checkboxами
- **Extracted Data Preview:** Предпросмотр найденных cookies и headers

**Что автоматически извлекается:**
- ✅ **Cookies:** Все cookies из `Cookie` заголовка
  - Формат: `session=abc123; token=xyz`
  - Автоматически парсятся и добавляются в сканер
- ✅ **Custom Headers:** Важные заголовки (кроме стандартных Host, Content-Length)
  - Authorization headers (Bearer tokens, Basic auth)
  - X-CSRF-Token, X-Requested-With
  - Custom API keys и токены
  - User-Agent (если нестандартный)

**После нажатия OK:**
1. Переключение на вкладку "Scan Configuration"
2. URL автоматически заполнен
3. Cookies автоматически заполнены в поле Cookies
4. Custom Headers автоматически заполнены в поле Custom Headers
5. Модули выбраны согласно вашему выбору
6. Консоль показывает подтверждение:
   ```
   🔍 Browser → Scanner: Configured scan for https://example.com/admin
      Modules: sqli, xss, csrf, idor
      Cookies: 3 auto-configured ✓
      Headers: 2 auto-configured ✓
      Click 'Start Scan' to begin
   ```

**Use Cases (Примеры использования):**

**1. Сканирование админ-панели:**
```
1. Авторизуйтесь как admin в браузере
2. Перейдите на /admin/users
3. Send to Scanner → выберите IDOR, CSRF, SQLi модули
4. Start Scan
→ Сканер будет тестировать /admin/users с вашей админ-сессией
```

**2. Тестирование API с Bearer token:**
```
1. Получите Bearer token через POST /api/login
2. Сделайте запрос к GET /api/users (с Authorization header)
3. Send to Scanner → выберите IDOR, SQLi модули
4. Start Scan
→ Сканер будет использовать ваш Bearer token для всех запросов
```

**3. Тестирование с CSRF token:**
```
1. Загрузите страницу с формой (получите CSRF token)
2. Отправьте POST запрос с X-CSRF-Token
3. Send to Scanner
→ Сканер будет включать CSRF token во все запросы
```

**Преимущества:**
- ⚡ **Быстрая настройка** - не нужно вручную копировать cookies и headers
- 🎯 **Точность** - все cookies и headers из реального запроса
- 🔐 **Аутентификация** - тестирование защищенных страниц без ручной настройки
- 🛡️ **Bypass защиты** - автоматически включаются все нужные headers для обхода WAF

**Совместимость:**
- ✅ Session-based аутентификация (cookies)
- ✅ Token-based аутентификация (Authorization header)
- ✅ API keys (X-API-Key, etc.)
- ✅ CSRF tokens (X-CSRF-Token)
- ✅ Custom headers (X-Requested-With, etc.)

---

### 🔒 Пассивное сканирование (Passive Scanning)

**Что это:**
Автоматический анализ запросов и ответов во время навигации по сайту.

**Что обнаруживает:**
- 🔑 Sensitive data (пароли, токены, API ключи)
- 🔓 Missing security headers
- 🍪 Небезопасные cookies
- 📧 Email addresses
- 🔗 Internal paths и endpoints
- ⚠️ Error messages с информацией

**Как использовать:**
1. Галочка **"Passive Scanning"** включена по умолчанию
2. Просто используйте браузер
3. Находки появляются в таблице **"Passive Scan Findings"** внизу
4. Цвет фона показывает severity (Critical, High, Medium, Low)

**Панель находок показывает:**
- **Severity** - Критичность с цветовой маркировкой
- **Type** - Тип находки (Sensitive Data, Security Header, etc.)
- **URL** - Где найдено
- **Evidence** - Доказательство (что именно найдено)
- **Description** - Описание проблемы

---

### 🎯 Сканирование страницы (Scan This Page)

**Как использовать:**
1. Посетите интересующий сайт через браузер
2. В истории запросов найдите нужную страницу
3. Кликните на неё
4. Выберите модули из выпадающего списка:
   - **All Modules** - Все модули
   - **XSS Only** - Только XSS
   - **SQLi Only** - Только SQL Injection
   - **LFI Only** - Только Local File Inclusion
   - **SSTI Only** - Только Server-Side Template Injection
   - **CMDi Only** - Только Command Injection
   - **SSRF Only** - Только Server-Side Request Forgery
   - **Critical Modules** - XSS, SQLi, LFI
5. Нажмите **"🔍 Scan This Page"**
6. GUI автоматически:
   - Заполнит target URL
   - Выберет нужные модули
   - Переключится на вкладку "Scan Configuration"
7. Нажмите "Start Scan" для начала сканирования

**Пример workflow:**
```
1. Запустите прокси
2. Запустите Chrome
3. Зайдите на http://testphp.vulnweb.com
4. Поклацайте по сайту (формы, страницы, параметры)
5. В истории найдите страницу с формой логина
6. Выберите "Critical Modules"
7. Нажмите "Scan This Page"
8. Запустите сканирование
```

---

### 🗑 Очистка истории (Clear History)

**Когда использовать:**
- Слишком много запросов (>1000 автоматически удаляются старые)
- Начинаете работу с новым сайтом
- Хотите сфокусироваться на новых запросах

**Как очистить:**
1. Нажмите **"🗑 Clear History"**
2. Подтвердите действие
3. Вся история и счетчики будут очищены

---

## Настройки прокси / Proxy Settings

### Изменение порта
**По умолчанию:** 8080

**Как изменить:**
1. Убедитесь что прокси **остановлен**
2. Измените значение в поле **"Port: XXXX"**
3. Запустите прокси снова
4. **Важно:** Если запускаете браузер вручную, укажите новый порт

### Ручная настройка браузера (если Launch Chrome не работает)

**Windows:**
```bash
"C:\Program Files\Google\Chrome\Application\chrome.exe" --proxy-server=127.0.0.1:8080
```

**Linux:**
```bash
google-chrome --proxy-server=127.0.0.1:8080
```

**macOS:**
```bash
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --proxy-server=127.0.0.1:8080
```

**Или используйте расширения:**
- FoxyProxy
- Proxy SwitchyOmega

---

## Примеры использования / Usage Examples

### Пример 1: Поиск XSS вручную через Intercept

```
1. Включите Intercept
2. Зайдите на форму поиска
3. Введите: test
4. Перехватите запрос
5. Измените параметр на: <script>alert(1)</script>
6. Forward
7. Смотрите ответ - сработал ли XSS
```

### Пример 2: Анализ API endpoints

```
1. Откройте веб-приложение с API
2. Используйте функционал сайта
3. В истории фильтруйте по методу POST
4. Смотрите все API вызовы
5. Replay с разными параметрами
6. Ищите уязвимости в API
```

### Пример 3: Обход аутентификации

```
1. Залогиньтесь на сайт через браузер
2. В истории найдите запрос логина
3. Replay для анализа
4. Попробуйте SQL injection в параметрах
5. Modify & Replay с: ' OR '1'='1
6. Проверьте обход
```

### Пример 4: Комплексный workflow

```
1. Start Proxy
2. Launch Chrome
3. Включите Passive Scanning
4. Посетите целевой сайт
5. Проходите по всем страницам, формам, функциям
6. Смотрите passive findings (утечки, небезопасные headers)
7. Находите интересные endpoints
8. Выбираете критичную страницу
9. Scan This Page с Critical Modules
10. Анализируете результаты активного сканирования
11. Для найденных уязвимостей делаете Replay для подтверждения
```

---

## Горячие клавиши / Keyboard Shortcuts

- **Ctrl+C** в таблице истории - Копировать URL
- **Double-click** на запросе - Показать детали
- **Delete** - Удалить выбранный запрос (TBD)

---

## Советы и трюки / Tips & Tricks

### 💡 Совет 1: Используйте фильтрацию
Кликните на заголовок столбца для сортировки (Method, Status, etc.)

### 💡 Совет 2: Комбинируйте с Burp Suite
Можете запустить оба прокси в цепочке:
- Dominator Proxy: 8080
- Burp Suite: 8081 → upstream 8080

### 💡 Совет 3: Сохраняйте интересные запросы
В столбце "Notes" можно добавлять комментарии (TBD feature)

### 💡 Совет 4: Passive + Active scanning
1. Сначала passive scanning найдет endpoints
2. Потом active scanning проверит их на уязвимости
3. Replay подтвердит находки

### 💡 Совет 5: Используйте для обучения
Это отличный способ понять как работают веб-приложения:
- Смотрите все HTTP запросы
- Анализируйте headers
- Изучайте структуру API
- Экспериментируйте с параметрами

---

## Troubleshooting / Решение проблем

### ❌ Chrome/Chromium не запускается
**Решение:**
1. При первом запуске появится предложение скачать Chromium (~150MB)
2. Нажмите "Yes" для автоматической загрузки
3. Если загрузка не удалась, система попробует использовать установленный Chrome
4. В крайнем случае настройте любой браузер вручную на прокси 127.0.0.1:8080

### ❌ Прокси не перехватывает запросы
**Решение:**
1. Убедитесь что прокси запущен (🟢)
2. Проверьте что браузер использует прокси (Settings → Proxy)
3. Проверьте порт (по умолчанию 8080)

### ❌ Не видно запросов в истории
**Решение:**
1. Проверьте что браузер настроен на прокси
2. HTTP запросы видны полностью
3. HTTPS запросы тоже видны (URL и заголовки), но тело зашифровано - это нормально

### ❌ Passive findings не появляются
**Решение:**
1. Проверьте что галочка "Passive Scanning" включена
2. Посетите страницы с формами, cookies, данными
3. Некоторые находки появляются только на определенных типах страниц

### ❌ Scan This Page не работает
**Решение:**
1. Убедитесь что выбрали запрос в истории (кликнули на него)
2. Проверьте что выбраны модули
3. После клика "Scan This Page" нужно нажать "Start Scan" на вкладке Scan Configuration

---

## Ограничения / Limitations

### Текущие ограничения:
- ⚠️ HTTPS работает через туннелирование (видны URL, но тело зашифровано - это нормально)
- ❌ WebSockets пока не поддерживаются
- ❌ Нет автоматического сохранения истории между сессиями
- ❌ Максимум 1000 запросов в истории (старые удаляются)

### ✅ НОВОЕ в последней версии:
- ✅ Встроенный Chromium - не нужна установка Chrome
- ✅ HTTPS туннелирование - HTTPS сайты работают
- ✅ Темная тема в диалогах - больше нет белого текста

### Планируемые улучшения:
- ✅ SSL certificate generation для HTTPS interception
- ✅ WebSocket support
- ✅ Экспорт истории в JSON/CSV
- ✅ Фильтрация истории (по domain, method, status)
- ✅ Match & Replace rules
- ✅ Auto-responder
- ✅ Сравнение запросов (diff)

---

## FAQ

**Q: Зачем этот модуль если есть Burp Suite?**
A: Интеграция! Вы можете сразу из браузера запускать полное сканирование Dominator на любую страницу. Плюс всё в одном инструменте.

**Q: Можно ли использовать вместе с Burp?**
A: Да! Настройте Burp на upstream proxy к Dominator.

**Q: Безопасно ли для production сайтов?**
A: Используйте только на тестовых средах или с разрешением! Это инструмент для пентеста.

**Q: Работает ли с мобильными приложениями?**
A: Да! Настройте мобильное устройство на использование прокси вашего компьютера (IP:8080).

**Q: Как остановить прокси?**
A: Нажмите "⏹ Stop Proxy" в интерфейсе.

---

## Видео-туториал (скоро)

Coming soon: Video demonstration of all features!

---

**Приятного тестирования! / Happy Hacking! 🚀**

*Generated with ❤️ by Claude Code for Dominator Scanner*
