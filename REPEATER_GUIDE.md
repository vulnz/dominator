# Repeater Tab - Руководство пользователя

## Что это такое?

**Repeater Tab** - это полный аналог Burp Suite Repeater, встроенный в Dominator Scanner.
Позволяет вручную редактировать и переотправлять HTTP запросы для тестирования уязвимостей.

---

## Как открыть Repeater?

### Способ 1: Через Browser Tab
```
1. Запустите GUI: python main.py --gui
2. Перейдите на вкладку "🌐 Browser & Proxy"
3. Увидите две под-вкладки:
   - "🌐 Proxy & Intercept"
   - "🔁 Repeater" ← ВОТ ОНА!
```

### Способ 2: Send to Repeater
```
1. В Proxy tab просматривайте историю запросов
2. Кликните на интересный запрос
3. Нажмите "🔁 Send to Repeater" (синяя кнопка)
4. Автоматически откроется Repeater с загруженным запросом
```

---

## Интерфейс Repeater

```
┌──────────────────────────────────────────────────────────────┐
│  Request Controls                                             │
│  [▶ Send Request]  [🗑 Clear]  [📁 Load]  Timeout: 10 sec    │
│  ☐ Follow Redirects                                          │
├──────────────────────┬───────────────────────────────────────┤
│  📤 Request          │  📥 Response                          │
│                      │                                        │
│  URL: [________]     │  ┌─ Raw ─ Headers ─ Body ─┐          │
│  Method: [GET ▼]     │  │                          │          │
│                      │  │  HTTP/1.1 200 OK         │          │
│  GET /api HTTP/1.1   │  │  Content-Type: ...       │          │
│  Host: example.com   │  │                          │          │
│  User-Agent: ...     │  │  Response body...        │          │
│                      │  │                          │          │
│  [+ Add Header]      │  └──────────────────────────┘          │
│  [✨ Beautify]       │                                        │
│                      │  Status: 200 OK | Size: 1234 B        │
│                      │  Time: 0.125s                         │
└──────────────────────┴───────────────────────────────────────┘
│  Status: Ready. Load a request or enter one manually.        │
│  History: 0 requests                                          │
└──────────────────────────────────────────────────────────────┘
```

---

## Основные функции

### 1️⃣ Редактирование запроса

**Два способа:**

**A) Быстрое редактирование (сверху):**
```
URL: [https://example.com/api/test]
Method: [POST ▼]
```

**B) Полное редактирование (raw HTTP):**
```
POST /api/test HTTP/1.1
Host: example.com
User-Agent: Dominator/1.0
Content-Type: application/json

{"username":"admin","password":"test123"}
```

### 2️⃣ Отправка запроса

```
1. Отредактируйте запрос
2. Нажмите большую зелёную кнопку "▶ Send Request"
3. Ждите ответа (справа)
```

**Настройки:**
- **Timeout:** 1-120 секунд (по умолчанию 10)
- **Follow Redirects:** следовать за редиректами (по умолчанию OFF)

### 3️⃣ Просмотр ответа

**3 вкладки:**

1. **Raw** - полный HTTP ответ:
   ```
   HTTP/1.1 200 OK
   Content-Type: application/json
   Content-Length: 42

   {"status":"success","data":"..."}
   ```

2. **Headers** - только заголовки:
   ```
   Content-Type: application/json
   Content-Length: 42
   Server: nginx/1.18.0
   ```

3. **Body** - только тело ответа:
   ```
   {"status":"success","data":"..."}
   ```

**Метаданные:**
- **Status:** 200 OK (цвет зависит от кода)
  - 🟢 2xx = зелёный
  - 🔵 3xx = синий
  - 🟠 4xx = оранжевый
  - 🔴 5xx = красный
- **Size:** размер ответа в байтах
- **Time:** время ответа в секундах

---

## Примеры использования

### Пример 1: Тестирование SQL Injection

```
1. Load запрос к форме логина из Proxy
2. В Repeater измените параметр:

   POST /login HTTP/1.1
   Host: example.com
   Content-Type: application/x-www-form-urlencoded

   username=admin' OR '1'='1&password=test

3. Send Request
4. Смотрите ответ - авторизовались ли?
5. Пробуйте разные payloads:
   - admin'--
   - admin' OR 1=1--
   - admin' UNION SELECT NULL--
```

### Пример 2: Обход аутентификации

```
1. Load запрос к защищённому API
2. Добавьте фейковый токен:

   GET /admin/users HTTP/1.1
   Host: api.example.com
   Authorization: Bearer fake_token_12345

3. Send Request
4. Проверьте - пустил ли сервер?
```

### Пример 3: Изменение User-Agent

```
1. Измените User-Agent:

   GET /api/data HTTP/1.1
   Host: example.com
   User-Agent: Mozilla/5.0 (iPhone; CPU iPhone OS 14_0)

2. Send Request
3. Проверьте - отдаёт ли мобильную версию?
```

### Пример 4: Тестирование IDOR

```
1. Load запрос к профилю:

   GET /api/user/123 HTTP/1.1

2. Измените ID:

   GET /api/user/124 HTTP/1.1

3. Send Request
4. Видите ли чужие данные? → IDOR уязвимость!
```

### Пример 5: Testing Rate Limits

```
1. Отправьте один запрос
2. Запомните response time
3. Быстро отправьте 10 запросов подряд
4. Проверьте:
   - Появился ли 429 Too Many Requests?
   - Увеличилось ли время ответа?
   - Заблокировал ли сервер?
```

---

## 🎉 НОВЫЕ ВОЗМОЖНОСТИ

### 📑 Множественные вкладки (Multiple Tabs)

Теперь вы можете работать с несколькими запросами одновременно!

**Как использовать:**
```
1. Кликните "➕ New Tab" в правом верхнем углу
2. Или отправьте запрос из Proxy в Repeater - создастся новая вкладка
3. Переключайтесь между вкладками
4. Закрывайте ненужные вкладки (крестик на вкладке)
```

**Управление вкладками (правый клик на вкладке):**
```
✏️ Rename Tab - переименовать вкладку
⭐ Add to Favorites - пометить важную вкладку
⭐ Unfavorite - убрать из избранного
❌ Close Other Tabs - закрыть все остальные вкладки
```

**Переименование вкладок:**
- Правый клик на вкладке → "✏️ Rename Tab"
- Введите новое имя (например: "Login Test", "SQLi Check")
- Легко находить нужные запросы среди множества вкладок

**Избранные вкладки:**
- Правый клик → "⭐ Add to Favorites"
- Звёздочка появится в названии вкладки
- Отмечайте важные запросы, которые часто используете
- Визуально выделяются среди других вкладок

**Пример:**
```
⭐ Login Test: POST /api/login
Tab 2: GET /api/users
⭐ SQLi Check: GET /api/search?q=test
Tab 4: DELETE /api/user/123
...и так до 100 вкладок!
```

### 🔄 Auto-Repeat (Автоповтор)

Отправляйте один запрос N раз для тестирования:

**Использование:**
```
1. Установите количество повторов (1-100)
2. Нажмите "🔄 Auto-Repeat" (оранжевая кнопка)
3. Смотрите прогресс в диалоге
4. Получите детальную статистику
```

**Статистика показывает:**
- ✅ Количество успешных запросов
- ❌ Количество ошибок
- ⏱ Среднее время ответа
- 📊 Распределение status codes

**Применение:**
- Тестирование rate limiting
- Проверка стабильности API
- Нагрузочное тестирование
- Анализ производительности

### 💾 Save/Load Requests

Сохраняйте и загружайте запросы!

**Сохранение:**
```
1. Нажмите "💾 Save" или Ctrl+S
2. Выберите имя файла (формат .http)
3. Запрос сохранён со всеми настройками
```

**Загрузка:**
```
1. Нажмите "📂 Load"
2. Выберите сохранённый файл
3. Запрос загружен полностью
```

**Что сохраняется:**
- URL и метод
- Все заголовки
- Тело запроса
- Timeout
- Follow Redirects настройка

**Применение:**
- Библиотека полезных запросов
- Шаблоны для тестирования
- Обмен запросами с командой

### 🔍 Поиск в запросах/ответах (Search)

Быстрый поиск текста в запросе или ответе!

**Как использовать:**
```
1. Нажмите Ctrl+F (фокус на поле поиска)
2. Введите текст для поиска
3. Используйте кнопки ⬆/⬇ для навигации
4. Видите счётчик совпадений (например: "3/15")
```

**Возможности:**
- Поиск в запросе (Request)
- Поиск в ответе (Response)
- Case-insensitive (игнорирует регистр)
- Wrap-around (циклический поиск)
- Автоматическая подсветка совпадений
- Показывает номер текущего совпадения

**Применение:**
- Быстрый поиск заголовков (Content-Type, Authorization...)
- Поиск параметров в JSON/XML
- Поиск токенов и session ID
- Поиск ошибок в ответе

### ⌨️ Горячие клавиши (Keyboard Shortcuts)

**В каждой вкладке:**
- **Ctrl+Enter** - Send Request (отправить)
- **Ctrl+L** - Clear (очистить)
- **Ctrl+S** - Save (сохранить)
- **Ctrl+F** - Focus Search (фокус на поиск)

**Управление вкладками:**
- **Ctrl+T** - New Tab (новая вкладка)
- **Ctrl+W** - Close Tab (закрыть вкладку)
- **Ctrl+D** - Duplicate Tab (дублировать вкладку)

---

## Syntax Highlighting

Repeater автоматически подсвечивает:

- **HTTP методы** (GET, POST, ...) - 🔵 синий жирный
- **Заголовки** (Header-Name:) - 🟢 зелёный жирный
- **URLs** (https://...) - 🔵 голубой курсив
- **Status codes** (HTTP/1.1 200) - 🟠 оранжевый жирный

---

## Кнопки и функции

### Request Panel (слева)

| Кнопка | Функция |
|--------|---------|
| **+ Add Header** | Добавляет шаблон заголовка |
| **✨ Beautify** | Форматирует запрос |

### Top Toolbar

| Элемент | Функция |
|---------|---------|
| **▶ Send Request** | Отправить запрос (зелёная, большая) |
| **🗑 Clear** | Очистить редактор |
| **📁 Load from History** | Выбрать из истории Repeater |
| **Timeout** | Настройка таймаута (1-120 сек) |
| **Follow Redirects** | Следовать за редиректами |

---

## Обработка ошибок

### Timeout
```
Если запрос длится > Timeout секунд:
→ Появится: "Request timed out after X seconds"
```

### Connection Error
```
Если сервер недоступен:
→ Появится: "Failed to connect: ..."
```

### Invalid Request
```
Если неправильный формат:
→ Появится: "Invalid HTTP request format"
```

---

## История Repeater

Repeater сохраняет историю всех отправленных запросов в текущей сессии и отображает их в нижней части вкладки:

```
┌────────────────────────────────────────────────┐
│ 📋 Request History                             │
├────────────────────────────────────────────────┤
│ GET https://example.com/api - 200 (0.12s)     │
│ POST https://api.test.com/login - 401 (0.08s) │
│ GET https://example.com/users - 200 (0.15s)   │
└────────────────────────────────────────────────┘
│ Ready | History: 3 requests                    │
└────────────────────────────────────────────────┘
```

**Использование:**
1. История автоматически обновляется после каждого запроса
2. Видите метод, URL, статус код и время ответа
3. **Двойной клик** на запрос в истории - загрузит его в редактор
4. История сохраняется только в текущей сессии

**Что показывается:**
- HTTP метод (GET, POST, PUT, DELETE...)
- Полный URL запроса
- Status code ответа (200, 404, 500...)
- Время выполнения в секундах

---

## Отличия от Burp Suite Repeater

### Что есть:
✅ Редактирование raw HTTP
✅ Quick URL/Method edit
✅ Send Request
✅ Response tabs (Raw/Headers/Body)
✅ Timeout настройка
✅ Follow Redirects
✅ Syntax highlighting
✅ История запросов с визуальным отображением
✅ Error handling
✅ **Multiple tabs support** (NEW!)
✅ **Auto-repeat (send N times)** (NEW!)
✅ **Save/Load requests** (NEW!)
✅ **Keyboard shortcuts** (NEW!)
✅ **Tab management** (NEW!)
✅ **Search in request/response** (NEW!)
✅ **Visible history panel** (NEW!)
✅ **Rename tabs & favorites** (NEW!)

### Чего пока нет (будущие версии):
❌ HTTP/2 support
❌ WebSocket support
❌ Request comparison (diff)
❌ Request params tab (URL parameters editor)
❌ Hex editor

---

## Советы и трюки

### 💡 Совет 1: Быстрое тестирование payloads

```
1. Load запрос в Repeater
2. Сохраните оригинал (Copy)
3. Меняйте payload → Send → смотрите ответ
4. Paste оригинал обратно
5. Повторяйте с другими payloads
```

### 💡 Совет 2: Проверка encoding

```
Тестируйте разные encoding:
- URL encoding: %27 вместо '
- Unicode: \u0027 вместо '
- Double encoding: %2527
- HTML entities: &#39;
```

### 💡 Совет 3: Манипуляция Content-Type

```
Меняйте Content-Type и смотрите что происходит:
- application/json
- application/xml
- text/plain
- multipart/form-data
```

### 💡 Совет 4: Testing HTTP методы

```
Меняйте метод на тот же endpoint:
GET /api/users → 200 OK
POST /api/users → 201 Created
DELETE /api/users → ???
PUT /api/users → ???
```

### 💡 Совет 5: Комбо с Proxy

```
Workflow:
1. Proxy → browse сайт
2. Proxy → находите интересный запрос
3. Send to Repeater
4. Repeater → тестируете payloads
5. Repeater → находите уязвимость
6. Proxy → Scan This Page для полного теста
```

---

## Troubleshooting

### ❌ "Invalid HTTP request format"
**Причина:** Неправильный формат запроса
**Решение:**
```
Первая строка должна быть:
METHOD PATH HTTP/1.1

Пример правильного:
GET /api/test HTTP/1.1
Host: example.com
```

### ❌ Request не отправляется
**Причина:** Пустой URL или неправильный формат
**Решение:**
1. Заполните URL в поле сверху
2. Или укажите полный URL в raw request

### ❌ Response пустой
**Причина:** Сервер не ответил или timeout
**Решение:**
1. Увеличьте Timeout
2. Проверьте URL (доступен ли сервер?)
3. Проверьте firewall

---

## Keyboard Shortcuts (планируются)

Будут добавлены в следующих версиях:
- Ctrl+Enter - Send Request
- Ctrl+L - Clear
- Ctrl+K - Load from History
- Ctrl+S - Save Request
- Ctrl+D - Duplicate Tab

---

## FAQ

**Q: Можно ли отправить несколько запросов подряд?**
A: Да, просто нажимайте Send Request несколько раз. История сохранится.

**Q: Поддерживается ли HTTPS?**
A: Да, полностью. Просто используйте https:// в URL.

**Q: Можно ли отправлять POST с файлами (multipart)?**
A: Да, просто укажите правильный Content-Type и boundary.

**Q: Сохраняется ли история между сессиями?**
A: Пока нет, только в текущей сессии. Сохранение на диск - в roadmap.

**Q: Можно ли сравнивать два response?**
A: Пока нет, эта функция запланирована.

**Q: Работает ли syntax highlighting для JSON/XML в body?**
A: Пока только для HTTP. JSON/XML highlighting - в roadmap.

---

**Приятного тестирования! 🔁**

*Repeater - ваш лучший друг для ручного тестирования уязвимостей!*
